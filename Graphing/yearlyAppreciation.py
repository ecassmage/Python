import random
import json
stg = json.load(open("settings.json", 'r'))


def randSeed(company):
    if company.marketCap < 1000000:
        pass
    else:
        pass
    return


def main(company, companies, marketSize):
    marketCap = company.marketCap
    shares = company.shares
    marketValue = company.marketValue
    state = company.state
    return
